	void nextGreatest(int arr[], int n) {
	    
	     int maxRight = -1;
	   for(int i = n-1; i >= 0 ; i--){
	       int temp = maxRight;
            if(arr[i] > maxRight){
                maxRight = arr[i];
            }
            arr[i]= temp;
	    }
}
